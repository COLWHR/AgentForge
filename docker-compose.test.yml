version: '3.8'

services:
  test_db:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: agentforge_test
    ports:
      - "5432:5432"

  test_redis:
    image: redis:7
    ports:
      - "6379:6379"